import Icon from "../Icon";

export default function ContactIcon({ showLabel, hoverLabel }) {
  return (
    <Icon label="Contact me" name="contact" showLabel={showLabel} hoverLabel={hoverLabel}/>
  )
}
