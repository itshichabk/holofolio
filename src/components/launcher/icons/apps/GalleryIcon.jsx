import Icon from "../Icon";

export default function GalleryIcon({ showLabel, hoverLabel }) {
  return (
    <Icon label="Gallery" name="gallery" showLabel={showLabel} hoverLabel={hoverLabel}/>
  )
}
