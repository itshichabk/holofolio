import Icon from "../Icon";

export default function MapsIcon({ showLabel, hoverLabel }) {
  return (
    <Icon label="My travel map" name="maps" showLabel={showLabel} hoverLabel={hoverLabel}/>
  )
}
