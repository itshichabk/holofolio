"use client";
import Icon from "../Icon";

export default function SettingsIcon({ showLabel, hoverLabel }) {
  return (
    <Icon label="Settings" name="settings" showLabel={showLabel} hoverLabel={hoverLabel}/>
  )
}
